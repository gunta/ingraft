{
  "name": "@vendor-subtree/skill",
  "version": "0.3.0",
  "description": "Agent skill wrapper for the vendor-subtree CLI.",
  "license": "MIT",
  "files": [
    "SKILL.md",
    "README.md",
    "LICENSE"
  ],
  "type": "module",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "check": "bun run lint && bun run format:check",
    "format": "oxfmt . --write",
    "format:check": "oxfmt . --check",
    "lint": "oxlint .",
    "lint:fix": "oxlint . --fix"
  }
}
