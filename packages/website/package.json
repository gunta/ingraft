{
  "name": "vendor-subtree-website",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "astro check && astro build",
    "check": "bun run lint && bun run format:check && bun run typecheck && bun run build",
    "dev": "astro dev",
    "format": "oxfmt . --write",
    "format:check": "oxfmt . --check",
    "lint": "oxlint .",
    "lint:fix": "oxlint . --fix",
    "preview": "astro preview",
    "typecheck": "astro check"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.39.2",
    "astro": "^6.3.1",
    "sharp": "^0.34.5"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9"
  }
}
