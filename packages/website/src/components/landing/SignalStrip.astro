---
import { capabilitySignals } from "../../data/landing.ts"
---

<div class="signal-strip" aria-label="Product capabilities">
  {
    capabilitySignals.map((signal) => (
      <div class="signal">
        <strong>{signal.title}</strong>
        <span>{signal.body}</span>
      </div>
    ))
  }
</div>
