---
title: Tooling Integration
description: Keep editors, linters, formatters, language analyzers, package managers, task runners, and build systems focused.
---

import ToolIntegrationIndex from '../../../../components/ToolIntegrationIndex.astro'

Vendored source is useful context, but it should not dominate everyday tooling.
`vendor-subtree` detects project surfaces before writing anything, keeps source
available for coding agents and LSPs where that matters, and narrows only the
tools that create noise.

<ToolIntegrationIndex />
